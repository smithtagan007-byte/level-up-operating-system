// Replaced by SubmissionSection — kept to avoid import errors during transition
export function MarkSubmittedButton(_props: {
  candidateId: string
  roleId: string
  clientId: string
  roleTitle: string
}) {
  return null
}
